[package]
name = "rust_digital_clock"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.27"
chrono = { version = "0.4", features = ["serde"] }
chrono-tz = "0.8"
rodio = "0.17" # Audio playback for alarms
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
